{
  "name": "dockview-e2e",
  "private": true,
  "description": "Cross-browser regression test: the PackedArea window-keydown listener must recognize its binds in both Chromium and Firefox.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0"
  }
}
