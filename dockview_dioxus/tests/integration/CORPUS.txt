# Previously-failing fuzz cases — one `seed size` per line (`#` starts a comment, blanks ignored).
# `fuzz` appends a minimal repro here automatically when it finds a NEW failure; fix the bug,
# commit the new line, and `regressions` replays every entry forever. Do not hand-edit the
# numbers unless you know what you're doing — the trailing comment is free-form context.
0 33  # resize_pair inverted clamp range when a sibling pair summed to < 2·MIN_CHILD_PCT (fixed in splitview::resize_pair)
